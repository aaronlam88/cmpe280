extends layout

block content
  section(id="banner")
    h1 Welcome to #{title}
  section(id="main" class="wrapper")
    div(class="inner")
      div(class="align-left")
        canvas(id='plane')
        script(src="/planeCtr.js")
    